/**
 * Automated Playwright captures were retired in favor of manually exported
 * desktop PNGs (fully loaded, accurate scroll positions).
 *
 * Assets live in: assets/images/restorative-kinetics/screens/desktop-*.png
 *
 * To reintroduce automation later, restore viewport + scroll logic here.
 */
console.log(
  "Skipping automated screenshots — case study uses manual desktop exports (screens/desktop-*.png)."
);
process.exit(0);
